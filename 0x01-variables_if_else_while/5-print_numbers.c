#include <stdio.h>

/**
 * main - prints all single digit numbers up to 10
 * starting from 0, followed ny a new line
 * Return: 0 success
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	print("\n")'
	return (0);
}
